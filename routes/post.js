const express = require("express");
const router = express.Router();
const Controller = require("../controller/post");

// router.get('/' , Controller.getAllPost)
// router.post('/' , Controller.createPost)
// router.put('/:slug' , Controller.updatePost)
// router.delete('/:slug' , Controller.delete)
// router.get('/:slug' , Controller.singlePost)
// router.post("/comment/:slug", Controller.comment);
// router.delete("/comment/:slug",  Controller.deleteComment);
// router.put("/likes/:slug", Controller.likeBlog);

module.exports = router;